#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-recess/bin/grunt-recess" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-recess/bin/grunt-recess" "$@"
  ret=$?
fi
exit $ret
